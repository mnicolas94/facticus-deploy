name: Build and Deploy

on:
  workflow_dispatch:
    inputs:
      json_parameters:
        type: string
        required: true

jobs:
  run:
    uses: mnicolas94/facticus-deploy/.github/workflows/workflow_call.yml@feat/ios_support
    with:
      json_parameters: ${{ inputs.json_parameters }}
    secrets: inherit